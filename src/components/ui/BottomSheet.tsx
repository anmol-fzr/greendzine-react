import {
  BottomSheet as RSBS,
  BottomSheetProps,
} from "react-spring-bottom-sheet";
import "react-spring-bottom-sheet/dist/style.css";

const BottomSheet = (props: BottomSheetProps) => <RSBS {...props} />;

export { BottomSheet };
